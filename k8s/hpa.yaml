apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: memorystack-server-hpa
  namespace: memorystack
  labels:
    app.kubernetes.io/name: memorystack-server
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: memorystack-server
  minReplicas: 2
  maxReplicas: 10
  metrics:
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: 70
    - type: Resource
      resource:
        name: memory
        target:
          type: Utilization
          averageUtilization: 80
  behavior:
    scaleDown:
      stabilizationWindowSeconds: 300
      policies:
        - type: Pods
          value: 1
          periodSeconds: 60
    scaleUp:
      stabilizationWindowSeconds: 0
      policies:
        - type: Pods
          value: 2
          periodSeconds: 60
        - type: Percent
          value: 100
          periodSeconds: 60
      selectPolicy: Max
---
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: memorystack-web-hpa
  namespace: memorystack
  labels:
    app.kubernetes.io/name: memorystack-web
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: memorystack-web
  minReplicas: 2
  maxReplicas: 5
  metrics:
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: 70
  behavior:
    scaleDown:
      stabilizationWindowSeconds: 300
      policies:
        - type: Pods
          value: 1
          periodSeconds: 60
