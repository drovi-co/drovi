// =============================================================================
// UNIFIED OBJECT COMPONENTS
// =============================================================================
//
// UI components for displaying and interacting with Unified Intelligence Objects (UIOs).
//
// These components support the cross-source intelligence platform where:
// "Someone commits to something in Slack. The deadline is discussed later by email.
// Context lives in a doc. Our system treats that as one object — not three tools."
//

export { EvidenceChain, type EvidenceSource } from "./evidence-chain";
export { type SourceBreadcrumb, SourceBreadcrumbs } from "./source-breadcrumbs";
export { Timeline, type TimelineEvent } from "./timeline";
