# Imperium macOS App

Native macOS command station scaffold for Imperium.

## Goals

- Three-pane command layout with keyboard-first navigation
- Dense data views for market and portfolio modules
- Realtime stream and alert panels

## Setup

1. Install XcodeGen if not installed.
2. From this directory run `xcodegen generate`.
3. Open the generated Xcode project.
