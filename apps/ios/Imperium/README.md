# Imperium iOS App

Native iOS client scaffold for Imperium.

## Goals

- SwiftUI shell for module navigation
- UIKit bridge points for high-frequency charts
- Secure session handling and notifications

## Setup

1. Install XcodeGen if not installed.
2. From this directory run `xcodegen generate`.
3. Open the generated Xcode project.
