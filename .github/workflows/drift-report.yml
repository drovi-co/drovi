name: drift-report

on:
  workflow_dispatch:
  schedule:
    - cron: "0 5 * * 1"

jobs:
  drift-report:
    runs-on: self-hosted
    steps:
      - uses: actions/checkout@v4
      - name: Run drift report
        run: python drovi-intelligence/scripts/drift_report.py
