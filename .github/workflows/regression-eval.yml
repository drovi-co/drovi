name: regression-eval

on:
  workflow_dispatch:
  schedule:
    - cron: "0 4 * * 1"

jobs:
  eval:
    runs-on: self-hosted
    steps:
      - uses: actions/checkout@v4
      - name: Run regression eval
        run: bash drovi-intelligence/scripts/run_regression_eval.sh
